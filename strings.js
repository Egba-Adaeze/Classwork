/*The given input is a string of multiple words with a single space 
between each of them. Abbreviate the name and return the name initials.*/

const name = "George Raymond Richard Martin";
function nameInitials (name)
{

      for (let i =0; i < name.length; i++)
      if (name[i] == ' ')
        //console.log(name[i +1].toUpperCase());
        str.charAt(0).toUpperCase() + str.slice();
}


nameInitials (name)